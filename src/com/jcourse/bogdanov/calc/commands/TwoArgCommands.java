package com.jcourse.bogdanov.calc.commands;

import java.util.*;

interface TwoArgCommands {// list of Two Arg Commands
    final static String DEFINE = "DEFINE";
    final static ArrayList<String> list = new ArrayList<>(Arrays.asList(DEFINE));
}
